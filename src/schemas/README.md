# schemas

## Purpose / Responsibilities

* Single source of truth for **data models and validation** across the AI Real Estate Deal Analyzer.
* Defines typed contracts for all agents, orchestrators, and core modules using **Pydantic v2** models.
* Ensures deterministic serialization/deserialization of inputs, listings, media artifacts, forecasts, and reports.
* Enables safe interchange between deterministic and AI-seamed components.

## Public APIs / Contracts

* **Imports (verified):**

  ```python
  from src.schemas.models import (
      # Finance
      FinancialInputs, FinancingTerms, OperatingExpenses, IncomeModel, UnitIncome,
      RefinancePlan, MarketAssumptions, FinancialForecast, YearBreakdown,
      PurchaseMetrics, RefiEvent, InvestmentThesis,
      # Listing & insights
      ListingInsights, ListingNormalized, AddressResult, PhotoInsights,
      DetectedLabelModel, ParkingSummary, ObservationProvenance,
      # Fetch & media
      FetchPolicy, HtmlSnapshot, MediaCandidate, MediaAsset,
      MediaFinderResult, MediaBundle, MediaInsights, IngestResult,
      # Market (V2)
      MarketSnapshot, MarketHypothesis, HypothesisSet, RegionalIncomeTable,
  )
  from src.schemas.labels import (
      RoomType, MaterialTag, AmenityLabel, DefectLabel, ConditionTag, ParkingType,
  )
  ```

  > `RunOptions` and `AppInputs` (runtime options + full payload) live in **`src/inputs/inputs.py`**, not here — see [`../inputs/README.md`](../inputs/README.md).

### Finance Models

| Model | Description |
| --- | --- |
| **FinancialInputs** | Root object containing financing, opex, income, refi plan, and market assumptions. Main input for `core/finance/engine.py`. Includes `income_is_estimated` and `capex_reserve_upfront`. |
| **FinancingTerms** | Loan assumptions: `purchase_price`, `closing_costs`, `down_payment_rate`, `interest_rate`, `amort_years`, `io_years`, `mortgage_insurance_rate`. All rates in **fractions [0–1]**. |
| **OperatingExpenses** | Yearly recurring costs: `insurance`, `taxes`, `utilities`, `water_sewer`, `property_management`, `repairs_maintenance`, `trash`, `landscaping`, `snow_removal`, `hoa_fees`, `reserves`, `other`, plus `expense_growth`. All non-negative. |
| **IncomeModel** | **Per-unit** revenue model: `units: list[UnitIncome]` (monthly `rent_month` + `other_income_month`), `occupancy`, `bad_debt_factor`, `rent_growth`. Unit count = `len(units)`. |
| **RefinancePlan** | Refi assumptions: `do_refi`, `year_to_refi` (refi at END of that year), `refi_ltv`, optional `exit_cap_rate`, and `market_cap_rate`. **`market_cap_rate` is inert — record-keeping only.** Its field description used to promise it was "also used for purchase if `cap_rate_purchase` is not provided"; the engine never did that (`engine.py` reads only `market.cap_rate_purchase`, then the computed NOI/P cap). Mission 2 task 3.2 dropped the false promise rather than implementing it: `market.cap_rate_purchase` is the documented purchase override, and letting a field in the *refinance* block silently set the purchase cap would also change what `market.cap_rate_floor` is tested against (F1). `exit_cap_rate`'s description was corrected in the same pass — the real fallback chain is `refi.exit_cap_rate -> market.cap_rate_purchase -> computed purchase cap`, never `market_cap_rate`. **No field was added, renamed, retyped or removed; the descriptions now match the engine, so no data migration is needed.** |
| **MarketAssumptions** | Cap-rate context: `cap_rate_purchase`, `cap_rate_floor`, `cap_rate_spread_target`, `cap_rate_drift`. `cap_rate_floor` and `cap_rate_spread_target` are both **named in the report** since Mission 2 task 3.2 (thesis rationale + Run Provenance appendix); `cap_rate_drift` drives the NOI valuation table the report renders. |
| **FinancialForecast** | Engine output: `years: list[YearBreakdown]`, `purchase: PurchaseMetrics`, optional `refi: RefiEvent`, returns, warnings. |
| **YearBreakdown** | Single-year snapshot: GSI, GOI, per-line OPEX + `total_opex`, NOI, `debt_service`/`principal_paid`/`interest_paid`, `cash_flow`, `dscr`, `ending_balance`, valuation fields `cap_rate_applied`/`est_value`/`ltv_pct`/`available_equity`, and `notes`. **Not** annotations for IO years or the refi year — `engine.py:212-215` writes `notes` only from `_apply_insight_modifiers`'s `insight_notes`, and only at `y == 1`; a forecast with `financing.io_years` set and a realized later-year refi has empty `notes` in every year (verified). See `core/reports/README.md`'s "Adjustments Applied" section for what actually populates `notes`, and its honesty caveat on reachability. |
| **PurchaseMetrics** | Acquisition economics: cap rate, cash-on-cash return, DSCR, debt service, acquisition cash, spread. |
| **RefiEvent** | Realized refinance event inside a forecast (year, valuation, new loan, cash-out). |
| **InvestmentThesis** | Verdict + rationale + improvement levers. Generated by `agents/chief_strategist.py`. |

### Listing, Media & Ingest Models

| Model | Description |
| --- | --- |
| **ListingInsights** | Address, inferred `title`, stated facts (`price`, `sqft`, `bedrooms`, `bathrooms`, `year_built` — all optional, `None` = not stated/parsed), amenities, condition tags, defects, notes — the analyst output consumed by the finance engine and rendered as the report's "As listed" line. Also carries `observations: list[ObservationProvenance]` (see below). |
| **ObservationProvenance** | Per-tag provenance for one `ListingInsights` amenity/condition/defect: `tag`, `kind`, `origin` (`listing_text` \| `photo_filename` \| `cv_provider` \| `llm` \| `unknown`), plus optional `detail` (the phrase/token/threshold that fired), `provider`, `provider_kind` (`heuristic_stub` \| `model`), `provider_version`, `source_image_sha`, and the CV pipeline's own `detection: DetectedLabelModel`. **Read `provider_kind` before claiming a tag was AI-observed** — every built-in CV provider today is a `heuristic_stub`. |
| **ListingNormalized** | Structured listing extraction (beds/baths/sqft/units/address/amenities) from text or HTML. |
| **AddressResult** | Parsed US/CA address (street, city, region, postal, country hint). |
| **PhotoInsights** / **DetectedLabelModel** / **ParkingSummary** | Per-image CV records and closed-set detections with rollups (rooms, amenities, defects, parking). `DetectedLabelModel.source` (`DetectionSource`) records whether a detector emitted the label or a file name claimed it; `unconfirmed_hint_counts` / `contested_hint_counts` hold the filename claims deliberately kept out of the rollups. |
| **FetchPolicy** / **HtmlSnapshot** | Network opt-in policy (robots, timeouts, cache, JS render) and cached fetch results. |
| **MediaCandidate / MediaAsset / MediaBundle / MediaFinderResult** | Media discovery → download → manifest pipeline types (sha256, dimensions, kinds). |
| **MediaInsights** | Media stats: counts, bytes, dimensions, orientation, duplicates (exact + perceptual), hero image. |
| **IngestResult** | End-to-end ingestion artifact: normalized listing + media bundle + photo insights + provenance. |

### Market Models (V2 — not yet wired into the main pipeline)

| Model | Description |
| --- | --- |
| **MarketSnapshot** | Baseline market state: `region`, `cap_rate`, `vacancy_rate`, `rent_growth`, `expense_growth`, `interest_rate`. All as **fractions**. |
| **MarketHypothesis** | Delta adjustments (absolute percentage points) applied to snapshot fields; includes rationale and prior weight. |
| **HypothesisSet** | Collection of `MarketHypothesis` with metadata, prior normalization, and determinism guarantees. |
| **RegionalIncomeTable** | Deterministic summary of income distributions for regional comps (p25/median/p75). Also carries `turnover_cost`/`str_multiplier` fields the schema still declares, but no production caller computes a real `str_multiplier` or renders either — see `src/market/README.md` "Gate 3 correction". |

### Label Ontology (`labels.py`)

Closed-set enums (`RoomType`, `MaterialTag`, `AmenityLabel`, `DefectLabel`, `ConditionTag`, `ParkingType`) plus deterministic normalizers from filenames/text and listing-field extractors (`extract_listing_common`, `detect_heating`, `detect_cooling`, amenity/defect text normalizers).

`find_amenities_in_text(text) -> dict[AmenityLabel, str]` and `find_defects_in_text(text) -> dict[DefectLabel, str]` return each hit **with the literal substring that matched** — that phrase is the only evidence a keyword match has, and it is what `ObservationProvenance.detail` records for `origin="listing_text"`. `normalize_amenities_from_text` / `normalize_defects_from_text` are unchanged in behaviour and now delegate to them (`set(find_*)`).

### Migration note — `ListingInsights.observations` (Mission 2, additive)

`observations` is **additive and optional** (`default_factory=list`). No existing field was renamed, retyped, or removed, and `amenities` / `condition_tags` / `defects` remain the contract every current consumer reads. A `ListingInsights` constructed without `observations` is still valid, so no producer or persisted payload needs updating. Producers that have been taught to populate it: `core/ingest/listing_parser.py` (text), `orchestrators/cv_tagging_orchestrator.py` → `agents/listing_analyst.py` (photos), `core/insights/synthesis.py` (`ingest-listing`), and `agents/crewai_components.py`'s LLM path. Builders live in `core/insights/provenance.py`.

Because `ObservationProvenance` is declared next to `DetectedLabelModel` (which it reuses) — i.e. *after* `ListingInsights` — `models.py` ends with an explicit `ListingInsights.model_rebuild()` to resolve that forward reference loudly at import.

### Migration note — `PhotoInsights.unconfirmed_hint_counts` (Mission 2 / M17, additive)

`unconfirmed_hint_counts` is **additive and optional** (`default_factory=dict`); nothing was renamed, retyped or removed, and a `PhotoInsights` built without it is still valid.

It exists because `amenity_counts` and `defect_counts` had come to mean two different things at once. Both counted "images exhibiting this label", but the label could have been produced by a detector examining pixels **or** by `core/cv/runner._augment_from_filename` reading the file's NAME. Those counts flow into `ListingInsights.amenities` / `.defects`, and those three lists are what `core/finance/engine._apply_insight_modifiers` reads to select OPEX and income rules — so a claim nothing measured could select a rule and move a number. A blank grey image named `mold_basement.jpg` produced `defects: ['mold_suspected']`.

A filename may **suggest**; only a detector that actually looked may **confirm** (see `core.cv.amenities_defects.DetectionSource`). A suggestion a provider **corroborated** is a detection with a second agreeing signal and stays in `amenity_counts`/`defect_counts`. A suggestion **no registered provider declares it can detect** is not scoreable at all — nothing measured it — so it lands here instead, **without a confidence**, and is surfaced to the reader through `ListingInsights.notes` rather than through a tag list.

> **Correction of record (Mission 2, G2-N1).** This note previously said a suggestion a provider *contradicted* "is scoreable and stays in `amenity_counts`/`defect_counts`". That was true of the code and wrong as a rule — see the `contested_hint_counts` note below, which supersedes it.

That is why it is a third dict and not more entries in the first two: the difference between "a detector reported it" and "a file name said it and nothing checked" is exactly what the reader and the finance rules must not conflate. `DetectedLabelModel.confidence` stays required for the same reason — an entry with no confidence is not a detection and must not be able to masquerade as one, so unconfirmed hints never enter `image_detections`.

A label counted here moves into `defect_counts`/`amenity_counts` automatically the day a provider declaring it is registered (`core.cv.amenities_defects.register_provider` / `register_onnx_provider`); no code change is required.

### Migration note — `PhotoInsights.contested_hint_counts` (Mission 2 / G2-N1, additive)

`contested_hint_counts` is **additive and optional** (`default_factory=dict`); nothing was renamed, retyped or removed, and a `PhotoInsights` built without it is still valid.

It is the sibling of `unconfirmed_hint_counts`, for the other half of the same rule. That field holds claims **nothing was able to check**; this one holds claims a detector that *can* see the label checked and **contradicted**.

Those used to stay in `amenity_counts`/`defect_counts` on the grounds that they carry a corroboration score (0.30) and so are "usefully weak". They are not weak where it counts: `_apply_insight_modifiers` selects OPEX and income rules by **membership** in `ListingInsights.amenities`/`condition_tags`/`defects` and never reads a confidence. Reproduced: a blank grey `garage.jpg`, with a registered detector that declares `parking_garage` and reported nothing, produced `insights.amenities: ['parking']` and moved Year-1 cash flow by **$1,105.80**. A claim a detector rejected is a *weaker* basis for moving a number than one nothing could check, not a stronger one.

So contested entries are now routed here, kept out of `image_detections` (which therefore means exactly "what a detector reported"), and surfaced to the reader through `ListingInsights.notes` — a different sentence from the unconfirmed one, because "nothing looked" and "something looked and disagreed" are different facts.

**Consumers to update:** any code reading `PhotoInsights.amenity_counts`/`defect_counts` and expecting contested entries there will now find them in `contested_hint_counts` instead. In-tree, that was `core/reports/photo_report.py`, `core/intelligence/deal_fusion.py` and `narrative_builder.py`, all of which wanted the new behaviour (none should score a rejected claim) — `narrative_builder.py` was subsequently deleted at Gate 3 (2026-08-05, see `CHANGELOG.md` "Removed"); the other two remain live. `orchestrators/cv_tagging_orchestrator.py` was a **separate producer** with its own copy of this rollup and was left unchanged by this pass, with a known-gap note here saying so — that gap is now closed: commit `06da901` (Mission 2, task 3.4 Task A / 3.5) taught it to route every detection through `is_uncorroborated_filename_claim`, the same predicate `core.insights.synthesis` uses, so a claim nothing measured or a claim a detector rejected stays out of `amenities`/`condition_tags`/`defects` on *both* producers now, surfaced instead through its own `unconfirmed_hints`/`contested_hints` rollup keys (sibling to, not the same shape as, `PhotoInsights`'s `unconfirmed_hint_counts`/`contested_hint_counts`).

### Migration note — `DetectedLabelModel.source` (Mission 2 / G2-N2, additive)

`source: DetectionSource` is **additive** with a default of `"pixels"`; no field was renamed, retyped or removed, and a payload without it validates exactly as before.

`core/cv/runner._augment_from_filename` had always written this marker onto the raw `DetectedLabel` mapping, but the model did not declare it and `model_config = ConfigDict(extra="ignore")` therefore **deleted it at the validation boundary**. `core/insights/synthesis` was left with nothing to branch on and stamped every surviving record `origin="cv_provider", provider_kind="model"` — asserting a detector had found what it explicitly reported it had not. That is M17's laundering, on the sibling producer, and unlike M17's original it moved a number.

The fix carries the marker rather than teaching the consumer to guess, because **a consumer cannot decline to vouch for evidence it was never handed** — and because the marker then protects any *future* producer's records automatically, which a consumer-only rule could not. `DetectionSource` is declared in `schemas/models.py` (the schema layer may not import `core`) and re-exported by `core.cv.amenities_defects` under the same name, so existing importers are unaffected and there is exactly one definition.

The default is `"pixels"`, matching the producer's documented contract that an absent marker means a provider emitted the label itself; the *unsafe* direction — an unrecognised filename state being promoted to a detector finding — is closed instead by `is_uncorroborated_filename_claim`, which tests "filename-derived AND NOT confirmed" so a fifth state added later lands in the cautious branch.

## Usage Examples

### Example 1 — Construct Financial Inputs (per-unit income)

```python
from src.schemas.models import (
    FinancialInputs, FinancingTerms, OperatingExpenses, IncomeModel, UnitIncome,
)

inputs = FinancialInputs(
    financing=FinancingTerms(
        purchase_price=500_000, closing_costs=10_000, down_payment_rate=0.25,
        interest_rate=0.055, amort_years=30, io_years=0,
    ),
    opex=OperatingExpenses(insurance=2400, taxes=6000, utilities=1800, water_sewer=1200,
                           repairs_maintenance=2400, property_management=4800),
    income=IncomeModel(
        units=[UnitIncome(rent_month=1200, other_income_month=50),
               UnitIncome(rent_month=1200)],
        occupancy=0.95, bad_debt_factor=0.97, rent_growth=0.03,
    ),
)
print(inputs.model_dump_json(indent=2))
```

### Example 2 — Market Snapshot + Hypothesis Set

```python
from src.market.snapshot import build_snapshot
from src.market.hypotheses import generate_hypotheses

snap = build_snapshot({"market": {
    "region": "Metro A", "cap_rate": 0.055, "vacancy_rate": 0.06,
    "rent_growth": 0.03, "expense_growth": 0.02, "interest_rate": 0.045,
}})
hs = generate_hypotheses(snap, seed=42)
print(hs.summary())
```

## Design Notes / Invariants

* **Rates and percentages**: always expressed as fractions (e.g., `0.05` = 5%); hypothesis deltas are absolute percentage points.
* **Currency**: all monetary fields in consistent nominal currency; no inflation adjustments.
* **Validation rules**: enforced via Pydantic validators (non-negative money, bounded fractions, closed-set enums).
* **Serialization**: deterministic order via field definitions; stable across runs.
* **Backward compatibility**: legacy `FinancialInputs`-root JSON is auto-translated to the structured shape by `src/inputs/inputs.py`.

## Dependencies / Optional Providers

* No runtime dependencies beyond **Pydantic v2**.
* Consumed by: `core/*`, `agents`, `orchestrators`, `cli`, `market`, and `inputs`.

## Test Strategy

* Validated indirectly across nearly all domain tests; direct coverage in `tests/unit/test_schemas_fetchpolicy_validation.py` and the media/listing test suites.
* Run:

  ```bash
  pytest -q --no-cov tests/unit tests/core
  ```

  `--no-cov` disables coverage for this subset run only; the project's ≥80% coverage gate
  (`pytest.ini`, `--cov-fail-under=80`) is enforced against the **full** `pytest` suite, not any
  one subset command.

## Cross-links

* Back to [Main README](../../README.md)
* Related folders:

  * [`../core/README.md`](../core/README.md)
  * [`../market/README.md`](../market/README.md)
  * [`../agents/README.md`](../agents/README.md)
  * [`../orchestrators/README.md`](../orchestrators/README.md)
  * [`../core/reports/README.md`](../core/reports/README.md)
  * [`../cli/README.md`](../cli/README.md)
  * [`../inputs/README.md`](../inputs/README.md)

---

_Last reconciled: 2026-08-05 against mission/2-wiring-gaps (Gate 3 VETO remediation, B3/B4/BLOCKER 2): Usage Example 1 did not run — `OperatingExpenses(...)` omitted the required `utilities`/`water_sewer` fields, reproduced as `pydantic_core.ValidationError: 2 validation errors`; fixed and re-executed (all Usage Examples now executed and passing, see `tests/schemas/test_readme_usage_examples.py`). The `contested_hint_counts` note's `orchestrators/cv_tagging_orchestrator.py` line said it was unchanged by the contested/unconfirmed-routing pass; commit `06da901` changed exactly that, so the line is corrected to describe what it does today. Unrelated to this doc but fixed in the same pass: `is_uncorroborated_filename_claim` (referenced two paragraphs up) was a deny-list keyed on the closed three-element `FILENAME_SOURCES` set, so a `source` value the function had never seen matched neither "contested" nor "unconfirmed" and fell through to "trusted" — the opposite of the "cautious branch by default" property this file already claimed for it; flipped to an allow-list of the two values that positively mean a detector emitted the label (`pixels`, `filename_confirmed`), no schema change. Earlier note: 2026-08-05 against mission/2-wiring-gaps (Gate 3 remediation): noted `narrative_builder.py`'s deletion in the `contested_hint_counts` migration note's consumer list — no schema content changed (`RegionalIncomeTable`'s `turnover_cost`/`str_multiplier` fields also stay defined as-is; Gate 3 found their *values* fabricated, not the schema, and this project treats `models.py` as additive-only, so the field-removal question is left open — see `src/market/README.md`). Earlier note: 2026-08-05 against mission/2-wiring-gaps (task 3.2 / OPD-4: `RefinancePlan.market_cap_rate` + `.exit_cap_rate` descriptions corrected to match the engine — description-only, no migration; `MarketAssumptions` row notes where the guardrails now surface in the report). Earlier note: 2026-08-05 @ 615aaaf (corrected — the previous stamp cited `a626e9d`, a tree that does not contain the per-tag provenance content it claimed to have reconciled: `ObservationProvenance` appears 0 times there and 6 times at HEAD. A provenance stamp that names the wrong tree is the defect this file documents, applied to itself. Guardian M21.) (added `ObservationProvenance` + `ListingInsights.observations` — per-tag provenance, additive-only, with a migration note; documented `labels.find_amenities_in_text` / `find_defects_in_text`). Earlier note: 2026-08-04 @ d18ee1a (Gate 2 VETO remediation, finding V4: `YearBreakdown.notes` was described as annotating IO years/refi year — false, and verified false by running a forecast with `financing.io_years` set and a realized refi (empty `notes` in all 10 years); corrected to describe what `engine.py:212-215` actually writes. Earlier note: 2026-08-03 @ 74c985c, `ListingInsights` row now lists the stated-facts fields; `YearBreakdown` row now lists notes/valuation fields previously omitted)._
